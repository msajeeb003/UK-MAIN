{
  "name": "quote-tool-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Browser E2E tests for the Insurance Quote Comparison Tool.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "fixtures": "python fixtures/generate_fixtures.py",
    "install:browsers": "playwright install --with-deps chromium webkit msedge"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.14.0"
  }
}
